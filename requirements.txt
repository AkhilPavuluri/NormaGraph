# NormaGraph — Python dependencies (backend + normagraph_core)
# Install from repo root: pip install -r requirements.txt
# Same dependency set is declared in pyproject.toml for: pip install -e .

-r backend/requirements.txt
-r normagraph_core/requirements.txt
